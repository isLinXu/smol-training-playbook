# 🚀 快速开始指南

## 项目概述
这是一个从 Hugging Face Space 拉取的训练手册，已配置为支持：
- ✅ 本地 HTML 文件直接访问（无需服务器）
- ✅ GitHub Pages 自动部署

## 立即开始

### 🖥️ 本地访问（推荐用于离线查看）

```bash
# 1. 进入项目目录
cd app

# 2. 安装依赖（首次运行）
npm install

# 3. 构建本地版本
npm run build:local

# 4. 打开HTML文件
open dist/index.html
```

### 🌐 GitHub Pages 部署

```bash
# 1. 推送到GitHub
git add .
git commit -m "Deploy training playbook"
git push origin main

# 2. 在GitHub仓库设置中启用Pages
# Settings → Pages → Source: GitHub Actions
```

## 📋 测试清单

运行测试脚本验证配置：
```bash
./test-local.sh
```

预期输出：
- ✅ CSS文件存在
- ✅ JavaScript文件存在  
- ✅ 资源目录存在
- ✅ HTML文件大小正常

## 🔧 可用命令

| 命令 | 用途 |
|------|------|
| `npm run dev` | 开发服务器（热重载） |
| `npm run build:local` | 构建本地HTML版本 |
| `npm run build:github` | 构建GitHub Pages版本 |
| `npm run preview` | 预览构建结果 |

## 📁 重要文件

- `app/dist/index.html` - 本地访问的主文件
- `astro.config.local.mjs` - 本地配置（无base path）
- `astro.config.mjs` - GitHub Pages配置（有base path）
- `.github/workflows/deploy.yml` - 自动部署配置

## 🎯 使用场景

**本地HTML访问适合：**
- 离线查看文档
- 分享给他人（发送整个dist文件夹）
- 嵌入到其他项目中

**GitHub Pages适合：**
- 在线分享链接
- 团队协作
- 自动更新部署

## 🆘 常见问题

**Q: 本地HTML文件打开后样式丢失？**
A: 确保使用 `npm run build:local` 而不是 `npm run build`

**Q: GitHub Pages部署失败？**
A: 检查仓库设置中Pages配置是否选择了"GitHub Actions"

**Q: 图片无法显示？**
A: 所有资源都在 `_astro/` 目录中，确保整个 `dist` 文件夹完整

---

🎉 **现在您可以享受训练手册了！**